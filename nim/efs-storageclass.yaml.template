# EFS StorageClass for NIMCache PersistentVolumes.
#
# provisioner: efs-ap — one EFS Access Point per PVC (isolated directory per model).
# Name "efs" matches the storageClass field in nim/nimcaches.yaml.
#
# EFS_FS_ID is populated from CDK output EfsFileSystemId by deploy-nim.sh.

kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: efs
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: ${EFS_FS_ID}
  directoryPerms: "777"
